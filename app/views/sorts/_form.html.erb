<div class="form">
<%= simple_form_for (@sort) do |f| %>
  <% if @sort.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sort.errors.count, "error") %> prohibited this sort from being saved:</h2>

      <ul>
      <% @sort.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="actions">
    <%= link_to '返回', sorts_path ,class:'btn btn-link btn-lg'%>
    <%= f.submit "新增/更新" ,class:'btn btn-link btn-lg'%>
  </div>
  <div class="field">
    <%= f.input :title ,label:"類別名稱：" %>
  </div>

<% end %>
</div>